Minimal mp3 player

###REQUIREMENTS###
Please run the requirements.txt by:

OPEN CMD AND ENTER
cd (folder directory)
pip install -r requirements.txt  (IF DOESNT WORK 'python -m pip install -r requirements.txt')


HAVE FUN
